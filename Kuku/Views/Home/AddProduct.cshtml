@model Kuku.Models.Recipe_Product
@{
    ViewBag.Title = "Add product to recipe";
}
<form asp-action="AddProduct" asp-controller="home" asp-route-recipeid="@Model.RecipeId">
    <div class="form-group">
        <input type="submit" value="Send" class="btn btn-default" />
    </div>
</form>